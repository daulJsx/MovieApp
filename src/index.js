import { initializeApp } from "./scripts/view/app.js";
import "./styles/input.css";

document.addEventListener("DOMContentLoaded", () => {
  // Panggil fungsi initializeApp setelah DOM sudah siap
  initializeApp();
});
